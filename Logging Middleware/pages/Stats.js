// Stats page that shows all shortened URLs and click data
import URLStats from "../components/URLStats";
import { Box } from "@mui/material";

const Stats = () => (
  <Box sx={{ p: 4 }}>
    <URLStats />
  </Box>
);

export default Stats;